# 📦 Файлы для загрузки на GitHub

## ✅ Обязательные файлы:

Загрузите эти файлы из `C:\website\switch-homebrew\`:

### 1. Makefile
```
Makefile
```

### 2. Исходный код
```
source/main.c
```
(Создайте папку `source` на GitHub, загрузите `main.c` внутрь)

### 3. GitHub Action
```
.github/workflows/build.yml
```
(Создайте папки `.github/workflows`, загрузите `build.yml` внутрь)

### 4. Игнорируемые файлы (опционально)
```
.gitignore
```

### 5. README (опционально)
```
README_GITHUB.md
```
(Переименуйте в `README.md` при загрузке)

---

## 📂 Итоговая структура на GitHub:

```
ваш-репозиторий/
├── .github/
│   └── workflows/
│       └── build.yml
├── source/
│   └── main.c
├── Makefile
├── .gitignore
└── README.md
```

---

## ⚠️ НЕ загружайте:

- ❌ `icon.jpg` (отключен в Makefile)
- ❌ Папку `build/` (создается автоматически)
- ❌ `.bat` файлы (только для Windows)
- ❌ `.sh` файлы (только для локальной сборки)
- ❌ Другие `.md` файлы с инструкциями

---

## 🚀 После загрузки:

1. Перейдите в **Actions**
2. Нажмите **"Run workflow"**
3. Дождитесь зеленой галочки ✅
4. Скачайте `ai-chat-switch.zip` из Artifacts
5. Внутри будет `ai-chat.nro`!
