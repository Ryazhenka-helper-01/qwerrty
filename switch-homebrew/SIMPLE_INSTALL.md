# 🎮 Простая установка компонентов Switch

## Проблема
В установщике DevkitPro нет галочек для Switch Development.

## ✅ Решение: Установка через MSYS2

### Способ 1: Автоматический (попробуйте сначала)

Запустите:
```
C:\website\switch-homebrew\install-switch-dev.bat
```

Нажимайте **Y** и **Enter** на все вопросы.

---

### Способ 2: Ручной (если автоматический не работает)

1. **Откройте MSYS2:**
   - Пуск → Найдите "MSYS2" → Запустите "MSYS2 MSYS"
   - Или запустите: `C:\devkitPro\msys2\msys2.exe`

2. **Скопируйте и вставьте эти команды по очереди:**

```bash
# Шаг 1: Инициализация ключей
pacman-key --init
```
Нажмите **Enter**, дождитесь завершения.

```bash
# Шаг 2: Заполнение ключей
pacman-key --populate msys2
```
Если спросит (Y/n) - нажмите **Y** и **Enter**.

```bash
# Шаг 3: Обновление системы
pacman -Syu
```
Если спросит - нажмите **Y** и **Enter**.
Если попросит закрыть терминал - закройте и откройте заново.

```bash
# Шаг 4: Установка Switch Development
pacman -S switch-dev
```
Нажмите **Y** и **Enter**.

```bash
# Шаг 5: Установка дополнительных компонентов
pacman -S devkitA64 libnx switch-tools
```
Нажмите **Y** и **Enter**.

3. **Проверка установки:**

```bash
# Проверьте, что файлы появились
ls /opt/devkitpro/devkitA64
ls /opt/devkitpro/libnx
```

Должны увидеть список файлов.

---

### Способ 3: Если MSYS2 не находится

1. **Найдите MSYS2:**
   ```
   C:\devkitPro\msys2\msys2.exe
   ```

2. **Запустите двойным кликом**

3. **Выполните команды из Способа 2**

---

## После установки

1. **Закройте MSYS2**

2. **Запустите компиляцию:**
   ```
   C:\website\switch-homebrew\direct-compile.bat
   ```

3. **Или вручную в MSYS2:**
   ```bash
   cd /c/website/switch-homebrew
   make
   ```

---

## Если ничего не помогает

Скачайте готовый .nro файл или попросите кого-то скомпилировать за вас.

Альтернатива: используйте GitHub Actions для автоматической компиляции онлайн.
